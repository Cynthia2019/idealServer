{
    "builds": [{
        "src": "idealServer/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "idealServer/wsgi.py"
        }
    ]
}